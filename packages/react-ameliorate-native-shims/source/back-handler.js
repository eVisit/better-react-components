"use strict";

export const BackHandler = new (class BackHandlerPolyfill {
  constructor() {}

  addEventListener(listener) {
    // TODO: Add back event handler
  }

  removeEventListener(listener) {
    // TODO: Add back event handler
  }
})();
